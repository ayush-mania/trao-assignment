// Batch entry point (Section 9): npm run evaluate -- --input <cases.json> --output <kits.json>
// Implemented in TRAO-16. Placeholder exits non-zero so a half-built repo cannot pass silently.
console.error('evaluate: not implemented yet');
process.exit(1);
